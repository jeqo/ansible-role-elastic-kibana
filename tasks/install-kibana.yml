---
#

- name: define repo
  template:
    src: kibana-yum.repo.j2
    dest: "/etc/yum.repos.d/kibana-{{ kibana_version }}.repo"

- name: import elastic key
  rpm_key: state=present key=https://packages.elastic.co/GPG-KEY-elasticsearch

- name: install kibana
  yum: name=kibana state=present
